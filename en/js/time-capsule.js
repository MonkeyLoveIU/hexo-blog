(()=>{let init=()=>{document.querySelector("article.h-entry")&&fetch("/time-capsule-data.json").then(r=>r.json()).then(capsules=>{var today=new Date;let todayStr=`${today.getFullYear()}-${String(today.getMonth()+1).padStart(2,"0")}-`+String(today.getDate()).padStart(2,"0");var capsule,today=capsules.find(c=>c.date===todayStr);today&&(capsules=document.getElementById("comments")||document.querySelector(".comment-content, #waline-comment"))&&((capsule=document.createElement("div")).className="time-capsule",capsule.innerHTML=`
          <div class="time-capsule-title">📜 时空胶囊 — "${today.title}"</div>
          <div class="time-capsule-message">${today.message}</div>
          <div class="time-capsule-date">🕰️ 这封信写于过去，于 ${todayStr} 开启</div>
        `,capsules.parentNode.insertBefore(capsule,capsules))}).catch(()=>{})};"loading"===document.readyState?document.addEventListener("DOMContentLoaded",init):init(),document.addEventListener("pjax:complete",()=>setTimeout(init,200))})();